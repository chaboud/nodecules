# nodecules (backend)

This is the Python backend package for [nodecules](../README.md). The
repo-root `README.md` is the user-facing documentation; this file exists
so `poetry`/`pip install -e ./backend` doesn't choke on a missing
readme reference in `backend/pyproject.toml`.
